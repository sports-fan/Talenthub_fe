import { makeStyles } from '@material-ui/styles'

export default makeStyles(theme => ({
  dateRangeFilter: {
    display: 'flex',
    alignItems: 'center'
  },
  datePicker: {
    marginRight: theme.spacing(3)
  },
  download: {
    height: '100%'
  }
}))
