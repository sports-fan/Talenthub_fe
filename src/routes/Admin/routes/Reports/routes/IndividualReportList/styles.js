import { makeStyles } from '@material-ui/styles'

export default makeStyles(theme => ({
  download: {
    height: '100%'
  },
  dateRangeFilter: {
    display: 'flex',
    alignItems: 'center'
  },
  datePicker: {
    marginRight: theme.spacing(3)
  }
}))
